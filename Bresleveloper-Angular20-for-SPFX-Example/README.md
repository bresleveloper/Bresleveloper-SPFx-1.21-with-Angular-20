# Bresleveloper Angular 20 for SPFX

#### Dont Forget

* `npm install` if you dup this repo
* `npm install @angular/elements --save` 
* build with `ng build --output-hashing=none`


#### some examples:

* `async` pipe
* `singal` 
* `ChangeDetectorRef` 
